  <td class="whitespace-nowrap px-3 py-5 text-sm text-gray-500">
    <div class="flow-root">
      <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
        <div class="inline-block min-w-full align-middle sm:px-6 lg:px-8">
          <table class="min-w-full divide-y divide-gray-300">
            <tbody class="">
              <% transactions.each do |transaction|%>
                <tr>
                  <td class="whitespace-nowrap w-40 py-1 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-0"><%= transaction.name.truncate(16) %></td>
                  <td class="whitespace-nowrap w-40 px-3 py-1 text-sm text-gray-500"><%= humanized_money_with_symbol(transaction.amount) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </td>