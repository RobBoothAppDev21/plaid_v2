# frozen_string_literal: true

RSpec.describe Account::FindNewAccounts do
  context "find new accounts for item" do
    # pending it "finds new accounts and adds to item" do
      # expect { described_class.call(item) }.to change(Account.count)

    end
  end
end